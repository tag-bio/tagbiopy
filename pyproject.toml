[build-system]
requires = ["setuptools>=46.1.0"]
build-backend = "setuptools.build_meta"

# Version is static: tagbiopy.__version__ in src/tagbiopy/__init__.py (read via setup.cfg
# `version = attr: tagbiopy.__version__`). Bump it there per change, like the R SDK's DESCRIPTION.
